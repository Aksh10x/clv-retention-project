# Pinned to versions actually installed and tested in this project's .venv
# (Python 3.14, macOS; xgboost needs `brew install libomp`, weasyprint
# needs `brew install pango` on macOS — see README setup)

pandas==3.0.5
numpy==2.5.2
scipy==1.18.1
python-dateutil==2.9.0.post0
openpyxl==3.1.5

Lifetimes==0.11.3
scikit-learn==1.9.0
xgboost==3.4.1
shap==0.52.0
joblib==1.5.3

matplotlib==3.11.1
seaborn==0.13.2

Jinja2==3.1.6
weasyprint==69.0

pytest==9.1.1
